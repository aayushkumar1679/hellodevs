import React from "react";

export default function ResponsiveBar() {
  return (
    <div className="w-full h-8 bg-[var(--bg-surface)] border-b border-[var(--border-subtle)] flex items-center justify-center">
      {/* Responsive viewport dragging handle */}
    </div>
  );
}
